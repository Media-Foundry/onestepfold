# Project Summary

## Goal
- OneStepFold is an open, MSA-free protein folding project.
- The active scientific target is an efficient all-heavy-atom protein monomer predictor, eventually using one Pairformer cycle and one structure evaluation.
- The active method question is now late-recycle refinement emulation, not MeanFlow or early-exit routing by default.

## Current Status
- The repository is on `main`; remote is `https://github.com/Media-Foundry/onestepfold.git`. The worktree is intentionally not clean: the user moved `docs/hpc.md` to untracked `docs/hpc2.md` and added untracked `docs/hpc3.md`; `pyproject.toml` has the DiamondHill environment compatibility edits (`scikit-learn` dev dependency and Hatch direct-reference metadata). Preserve these changes.
- Full Stage A catalog is accepted: 244,406 SIFTS-linked PDB entries, deterministic shards, no parser errors, no unresolved SIFTS rows.
- Full Stage B is accepted: 84,232 `monomer_clean` records, 348 tar shards, 168,464 expected members, zero materialization/shape/mask/finite-coordinate errors.
- Frozen quality policy yields 78,259 train-valid records, 45,815 HQ-eval records, and 5,973 rejects.
- Quality-filtered exact groups: 38,400. Time split at initial release `2021-09-30`: 31,689 train-seen groups, 6,711 unseen temporal-test groups, 932 leakage-excluded same-sequence post-cutoff groups.
- Main ESMC-600M final-layer BF16 cache is complete for all 38,400 unique sequences. ESMC-300M ablation cache is also complete on the Precision W7900 system.
- Stage 0A--0C/0D/0E diagnostics are complete. Stage 0 findings: recycle depth dominates latency and quality; diffusion steps are largely redundant; fixed `c2_s2` is the strongest simple operating point; cheap cycle-1/trajectory routing does not close the joint all-atom risk gap.
- Stage 1A residual characterization is complete on 1,024 temporal-dev targets. Late residuals are broad and long-range; pair residual magnitude envelopes are highly low-rank, but this does not imply signed tensor low rank.
- Stage 1C signed 256-target diagnostic is complete. Adjacent residual directions are not aligned (pair cosine medians about `-0.086` and `-0.131`; single about `-0.122` and `-0.289`). Signed sketch rank-8 energy is about `0.82`, so simple fixed-point extrapolation is a negative baseline only.
- A C-alpha/global coordinate-refiner baseline exists in `src/onestepfold/models/coordinate_refiner.py`; it is not yet trained and is not an all-atom replacement.
- Corrected teacher-pair input `teacher_pairs_v3` has 16,000 legal standard-20-AA train groups in 16 shards. 320 groups containing unsupported `X/U/B/Z` symbols are preserved in an exclusion manifest.
- Corrected c2 generation is complete across two systems. hpc2 array `12771048` tasks 0--11 all completed successfully; tasks 12--15 were cancelled with zero elapsed time only after their hpc3 replacements passed QA. The hpc2 c4 array `12771049` was likewise cancelled after DiamondHill full c4 QA passed and never ran.
- DiamondHill now has a validated `fold` Conda environment at `/home/pc/anaconda3/envs/fold`, cloned from `AIAA` and supplemented with the pure-Python Protenix 1.1.0 dependency stack. All 44 repo tests passed, BF16 matmul passed on all eight MI250 GCDs, and the coordinate-refiner GPU forward/backward optimizer smoke passed. Protenix must use `LAYERNORM_TYPE=torch` plus torch triangle kernels; the CUDA fused layernorm path is incompatible with ROCm.
- The v3 inputs are present at `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/input`. A one-target `c4_s2` DiamondHill smoke completed successfully in 73.45 seconds wall time, produced a parseable CIF and finite confidence JSON with `num_recycles=4`, and peaked at about 18.3 GB resident memory.
- DiamondHill full c4 generation completed successfully under `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill`. Wave 1 passed QA at 16:00 HKT, wave 2 ended at 17:06:35, and exhaustive QA passed at 17:07:43. `c4_full_qa.json` reports `complete: true`, 16,000 expected and unique group IDs, and zero missing files, bad confidence JSON, bad CIF, or log issues. The durable pass marker is `output_diamondhill/control/c4_full_qa.PASSED`.
- hpc3 now has user Conda env `fold` at `/data/user/shuang886/.conda/envs/fold`: Python 3.12.11, PyTorch 2.7.1+cu128, Protenix 1.1.0, and the editable repo at `/data/user/shuang886/Folding/projects/onestepfold`. All 44 tests pass. GPU smoke job `628495` completed on an H100 80GB with finite BF16 forward/backward. Full Protenix c4_s2 smoke job `628527` completed on H100 in 75 seconds with zero errors, a parseable 814-atom CIF, and finite confidence JSON. Against the same DiamondHill target, pLDDT differed by about 0.032, pTM by 0.00019, and aligned all-atom RMSD was 0.0117 A.
- hpc3 has a validated runtime copied DiamondHill-to-hpc3 at `/data/user/shuang886/Folding/runtime_cache/protenix_v1_1` (6.5 GB). Environment manifests are under `/data/user/shuang886/Folding/projects/onestepfold/env_manifests/hpc3-fold-*-20260917.txt`.
- To accelerate c2, hpc3 ACD jobs `628767`, `628768`, `628769`, and `628770` produced shards 12--15 respectively on H100 GPUs in `acd_ue`. All completed with exit code 0 in 20:39--21:34. Inputs are under `/data/user/shuang886/Folding/stage1b/teacher_pairs_v3/input_hpc3`; isolated outputs are under `.../output_hpc3`. The discarded low-priority submissions `628761--628764` never ran and were cancelled before resubmission.
- The hpc3 watcher completed successfully at 19:16:51 HKT. `c2_shards_12_15_qa.json` reports `complete: true`, 3,970 expected/unique targets, and zero missing files, bad confidence JSON, bad CIF, or log issues. It then cancelled hpc2 array elements `12771048_12` through `_15`; `hpc2_12_15_cancel_confirmed` exists.
- The mixed-source teacher corpus is consolidated and accepted on DiamondHill under `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill`: c2 shards 0--11 came from hpc2 job `12771048_[0-11]`, c2 shards 12--15 came from hpc3 jobs `628767--628770`, and all c4 shards were generated on DiamondHill. Full paired validation found 16,000/16,000 c2 and 16,000/16,000 c4 predictions with no missing group IDs. Deep QA parsed all 32,000 CIFs, validated every confidence JSON and its expected recycle count, and checked all shard logs; missing, bad-confidence, bad-CIF, and log-issue counts are all zero. The durable pass marker is `output_diamondhill/control/paired_mixed_qa.PASSED`.
- Consolidation was copy-only, excluded transient `work` directories, and did not move or modify EngramFold, shared protein data, or Protenix checkpoints. The local relay `/tmp/onestepfold-c2-relay-20260917` was removed after acceptance.
- `teacher_pair_manifest_v1` is frozen on DiamondHill with 14,400 train and 1,600 validation exact groups, using seed 101 and a versioned SHA256 group ordering. The frozen temporal test was not read. Exhaustive loader QA passed 16,000/16,000 records, strictly aligned 37,321,433 c2/c4 atoms, and found zero missing N/CA/C/O backbone atoms. The manifest SHA256 is `084fbc88f8302eb770a021e593ff94b47a76e48cc38b079c96f00c58e7188325`.
- The accepted training data is now mirrored and revalidated on hpc3 at `/data/user/shuang886/Folding/stage1b/teacher_pairs_v3`. The compact teacher output has 64,094 files and 7,608,200,910 bytes; its relative-path/size digest matches DiamondHill (`7e57e87a...ada16e`). hpc3 paired validation found all 16,000 c2 and 16,000 c4 predictions. The existing hpc3 ESMC-600M cache covers all 16,000 teacher groups with zero missing or length mismatches; no ESMC recomputation was needed. The durable acceptance files are `output_diamondhill/control/hpc3_training_data_acceptance.json` and `hpc3_training_data.PASSED`.
- The hpc3 coordinate-refiner training path is implemented and its 32-target overfit gate passed. `ESMCShardStore` lazily slices existing BF16 safetensors; c4 C-alpha targets are Kabsch-aligned into the c2 frame; length-aware batching preserves masks. H100 job `629419` completed in 1:23. On the 32 shortest train groups (length 21--39), aggregate c2 baseline CA-RMSD was 1.2432 A and best training CA-RMSD was 0.1712 A at epoch 255. This is a pipeline/capacity result, not validation generalization.
- Frozen-data one-shot rsyncs were started from hpc2 to hpc3 `/data/user/shuang886/Folding` and DiamondHill `/media/PM982/onestepfold`; they intentionally exclude active `stage1b/teacher_pairs_v3` output and are not continuous synchronization.

## Decisions
- Use PDB/mmCIF experimental coordinates as GT; UniProt/SIFTS are provenance and traceability only.
- Keep the universal GT schema multi-chain-aware, but use a monomer-first training view: exactly one generated protein chain instance, no nucleic-acid or other-polymer chain instances, primary X-ray/EM/neutron method, one coordinate model, length 20--1024.
- Preserve missing residues/atoms as masks; do not silently impute them. Keep modified residues and mask unmapped modified side chains.
- Store exact 100% sequence identity grouping only. Preserve near-identical constructs and small mutations; keep each exact group in one split.
- Authoritative SI implementation is Biopython `Bio.Align.PairwiseAligner`; exact SHA256 is only a lossless candidate index.
- Main sequence conditioner is frozen ESMC-600M final residue layer, with pinned model/code revisions and BF16 sharded safetensors. ESMC-300M is an ablation; ESM3 structure-track outputs are excluded.
- Official Protenix Mini-ESM remains a compatibility baseline, not the active ESMC model.
- Do not tune on `frozen_temporal_test`; use `temporal_dev_v1` for all method selection. Keep the 15-group strict low-homology set frozen.
- Do not claim thermodynamic truth, sequence-pretraining cleanliness, or novel early stopping. Report the declared PDB-label cutoff and ESMC contamination limitation.
- Teacher-pair jobs must run each shard in a private working directory because Protenix writes relative `./esm_embeddings`. Any logged per-target `Data error` must make the Slurm task fail.
- Run DiamondHill teacher shards one per MI250 GCD (`ROCR_VISIBLE_DEVICES=0..7`), in two waves. Keep their output independent from the hpc2 mirror.
- Per explicit user instruction, do not cancel hpc2 c4 array `12771049` until all 16 DiamondHill shards finish and the full QA marker/result proves complete. Any supervisor or QA failure preserves the hpc2 jobs.
- The user explicitly permits installing Gemmi because it has no dependency burden. hpc3 `fold` currently has Gemmi 0.7.5; its import, CIF parsing, repo tests, and Protenix smoke are validated.
- Accept the mixed hpc2/hpc3 c2 corpus for paired teacher analysis because every artifact was revalidated uniformly on DiamondHill. Preserve source/job provenance per shard; this acceptance establishes artifact integrity, not numerical equivalence between GPU backends.
- Freeze the Stage 1B teacher split at 14,400/1,600 exact groups. The loader must verify sequence and c2/c4 atom-key identity, retain explicit missing-atom masks, require the expected recycle counts, and never impute prediction coordinates.
- The first refiner target is c4 C-alpha rigidly Kabsch-aligned into the c2 C-alpha frame. This removes arbitrary global rigid transforms without changing internal geometry. Treat the 32-target overfit as an implementation gate only.
- Per explicit user instruction, run all subsequent OneStepFold training on hpc3. DiamondHill is the accepted source/archive for the teacher corpus, not the training host. Reuse the existing hpc3 ESMC-600M cache rather than recomputing features.

## Constraints
- Raw/derived HPC data lives under `/hpc2hdd/home/shuang886/Folding`; Precision W7900 data is under `/media/WDisk/Datasets/OneStepFold`.
- DiamondHill project/data roots are `/media/PM982/projects/onestepfold` and `/media/PM982/onestepfold`; hpc3 data root is `/data/user/shuang886/Folding`.
- hpc3 training roots are `/data/user/shuang886/Folding/stage1b/teacher_pairs_v3/output_diamondhill` for paired coordinates and `/data/user/shuang886/Folding/esmc_600m_final_v1` for frozen ESMC-600M features.
- On hpc3, activate with `module load anaconda3; conda activate fold`. For Protenix use `LAYERNORM_TYPE=torch`, torch triangle kernels, and `PROTENIX_ROOT_DIR=/data/user/shuang886/Folding/runtime_cache/protenix_v1_1`.
- Use Slurm arrays as independent one-GPU jobs. Do not request an eight-GPU whole-node allocation for independent inference points.
- `i64m1tga800ue` is the preferred A800 partition; teacher jobs request 8 CPUs, 64G, one A800, and 8h. Adjust only after observing actual runtime.
- The local repo is the source of code/docs/reports. Push material changes to `origin/main`.
- Preserve the legacy `src/fastglycan` package; active protein code is under `src/onestepfold`.

## User Preferences
- Verify HPC state and output files directly over SSH; Slurm `COMPLETED` is not sufficient without artifact QA.
- Prefer parallel independent jobs and deterministic, resumable artifacts.
- Keep research claims narrower than diagnostic evidence; record material decisions in `.agents/memory/decision_log.md`.
- Use concise, evidence-backed updates and report blockers plainly.
- Always respond to the user in Chinese.

## Important Files
- `docs/hpc2.md`, `docs/hpc3.md`: current partition, resource, and launch guidance. The old `docs/hpc.md` was moved by the user.
- `docs/gt_protocol_v1.md`, `schemas/gt_schema_v1.json`: frozen GT contract.
- `configs/monomer_view_v1.toml`, `configs/gt_quality_v1.toml`: monomer and quality policies.
- `docs/esmc_cache_v1.md`: ESMC cache contract and pinned revisions.
- `scripts/select_teacher_pairs.py`: deterministic teacher-group selection and standard-AA filtering.
- `scripts/slurm_teacher_pair.sh`: isolated teacher shard runner and error-to-nonzero enforcement.
- `scripts/submit_teacher_pair_array.sh`: two-setting Slurm array launcher.
- `scripts/validate_teacher_pairs.py`: c2/c4 paired artifact validator.
- `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill/control/paired_validation_mixed.json`: 16,000-group c2/c4 presence validation.
- `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill/control/paired_deep_qa_mixed.json`: exhaustive CIF, confidence, recycle-count, and log QA for both settings.
- `/media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill/control/paired_provenance_mixed.json`: per-system and per-job source provenance for the accepted mixed corpus.
- `src/onestepfold/data/teacher_pairs.py`: frozen manifest schema and lazy, strict paired-coordinate loader.
- `src/onestepfold/data/teacher_pair_training.py`: ESMC shard slicing, Kabsch target construction, and length-aware batching.
- `scripts/train_coordinate_refiner.py`, `scripts/slurm_coordinate_refiner_overfit.sh`: hpc3 training entrypoint and overfit launcher.
- `reports/stage1b_coordinate_refiner_overfit32_2026-09-17.{json,md}`: accepted small-set capacity gate.
- `scripts/build_teacher_pair_manifest.py`: deterministic exact-group split builder tied to accepted QA/provenance inputs.
- `scripts/validate_teacher_pair_manifest.py`: exhaustive loader-level validator.
- `reports/stage1b_teacher_pair_manifest_2026-09-17.{json,md}`: frozen split and loader-QA acceptance record.
- `/data/user/shuang886/Folding/stage1b/teacher_pairs_v3/output_diamondhill/control/hpc3_training_data_acceptance.json`: hpc3 copy, ESMC coverage, and joint-read acceptance record.
- `scripts/protenix_residual_sitecustomize.py`: Stage 1 residual and optional signed-sketch hook.
- `reports/stage0_eval_summary_2026-09-16.md`: Stage 0 quality surface.
- `reports/stage0e_router_analysis_2026-09-16.md`: final router diagnostic.
- `reports/stage1a_residual_analysis_2026-09-17.md`: length-controlled residual analysis.
- `reports/stage1c_signed_residual_analysis_2026-09-17.json`: signed direction results.
- `reports/stage1c_signed_sketch_rank_analysis_2026-09-17.json`: signed sketch rank results.

## Commands
- Local tests: `pytest -q` (currently passes with one expected skip).
- Check repo: `git status --short`; latest fixes are `79da1e8`, `d967818`, and `f250626`.
- On hpc2, monitor: `module load slurm; squeue -u shuang886; sacct -X -j 12771048,12771049 --format=JobID,State,ExitCode,Elapsed,AllocCPUS,MaxRSS,NodeList`.
- Validate formal outputs after completion:
  `PYTHONPATH=/hpc2hdd/home/shuang886/Folding/catalog_pilot_code/src python /hpc2hdd/home/shuang886/Folding/catalog_pilot_code/scripts/validate_teacher_pairs.py --input-root /hpc2hdd/home/shuang886/Folding/stage1b/teacher_pairs_v3/input --output-root /hpc2hdd/home/shuang886/Folding/stage1b/teacher_pairs_v3/output_isolated --output-json /hpc2hdd/home/shuang886/Folding/stage1b/teacher_pairs_v3/validation.json`.
- A valid teacher corpus requires 16,000 c2 files, 16,000 c4 files, zero missing members, and no data-error logs.
- Build the frozen paired manifest with `scripts/build_teacher_pair_manifest.py`; validate the actual training-data contract with `scripts/validate_teacher_pair_manifest.py --workers 8` in an environment containing NumPy and Gemmi.
- DiamondHill control/status: `ssh pc@DiamondHill 'tail -n 50 /media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill/control/supervisor.log; tail -n 50 /media/PM982/onestepfold/stage1b/teacher_pairs_v3/output_diamondhill/control/full_qa_watcher.log'`.
- hpc2 Slurm is not on hpc3's default noninteractive PATH; query through hpc3 with `/opt/slurm/bin/squeue` or load the `slurm` module explicitly.
- hpc3 Slurm commands are in `/bin`; validated jobs are `628495` (CUDA/BF16) and `628527` (full Protenix smoke). The hpc3 GPU node exposed an NVIDIA H100 80GB and CUDA 12.8.

## Open Issues
- The old formal arrays `12768989/12768990` are invalid and must not be used: shared ESM temporary files caused partial, mismatched outputs. The first isolated retry also exposed unsupported amino-acid symbols and was cancelled.
- The Protenix runner can log `Data error` while exiting zero; always use wrapper exit status plus validator counts.
- Stage 1B generation, artifact QA, exact-group split, and loader QA are complete. The current gate is wiring the accepted loader to ESMC features and the global coordinate-refiner training loop.
- No full c2-to-c4 emulator has been trained yet. First analyze/fit cheap global coordinate residual baselines against paired teacher outputs.
- DiamondHill internal SSH/data transfer is healthy. Default routes and DNS work, and Anaconda returned HTTP 200; earlier `Errno 101`/PyPI SSL EOF events were intermittent outbound instability, so prefer the local wheel cache. Network is not the active compute bottleneck.

## Next Steps
1. Add an epoch-scale hpc3 trainer using deterministic length buckets, validation-only checkpoint selection, and resumable checkpoints for the 14,400/1,600 split.
2. Run a bounded train/validation pilot before the full schedule; compare validation CA-RMSD and distance metrics against uncorrected c2.
3. Compare the selected full model against fixed `c2_s2` and full `c4_s2`; keep the frozen temporal test untouched until model and calibration choices are locked.

## Handoff Notes
- Do not restart dataset construction or ESMC extraction; those layers are accepted and frozen.
- DiamondHill c4 is complete and accepted by exhaustive artifact QA; do not restart or create a competing launcher. hpc2 c4 `12771049` has already been cancelled as agreed.
- Teacher computation, consolidation, and full paired validation are complete. The accepted DiamondHill corpus combines hpc2 c2 shards 0--11, hpc3 c2 shards 12--15, and DiamondHill c4 shards 0--15; do not restart inference unless the data contract changes deliberately.
- The exact-group split and loader are now accepted too. Start from `teacher_pair_manifest_v1`; do not create an alternative split or touch the frozen temporal test during refiner development.
- hpc3 is the sole training host for the next stage. Its accepted data marker must be present before launching a training job; do not fall back to DiamondHill training merely because it retains the source copy.
- The 32-target overfit checkpoint is diagnostic and must not be promoted as a trained model. Use it only to debug the established input/target/training path.
- `engramfold/` is an independent shared project that also consumes protein data and Protenix checkpoints. Do not move or modify it or its shared assets while working on OneStepFold unless the user explicitly requests cross-project changes.
- The project has intentionally stopped treating MeanFlow and predictive routing as the default method. The evidence supports “skip expensive late recycles by predicting their refinement,” with a cheap global coordinate refiner as the first test.
